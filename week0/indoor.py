#prompts the user for input and then outputs that same input in lowercase. Punctuation and whitespace should be outputted unchanged.
string = input("Type the text you want to convert to lowercase \n ")
print(string.lower())
